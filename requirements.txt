flask>=3.0
# requests：qrcode/115 二维码图片抓取依赖（javdb/pan115.py 顶层 import，缺失会导致整个应用无法启动）
requests>=2.31
# h2：CloudDrive2 集成使用的纯 Python HTTP/2（h2c gRPC），缺少会导致整个应用无法启动
h2>=4.1
# p115client：115 网盘离线推送。可注释掉（此时推送自动降级禁用），但 115 推送功能不可用
p115client>=0.0.9.6.5.1
